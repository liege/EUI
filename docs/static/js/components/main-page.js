import React, { Component } from 'react';

export default class MainPage extends Component {
  constructor(...args) {
    super(...args);
  }

  render() {
    return (
      <div>Main page</div>
    );
  }
}