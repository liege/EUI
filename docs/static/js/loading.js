import React, { Component } from 'react';

export default class Loading extends Component {
  constructor(...args) {
    super(...args);
  }

  render() {
    return (
      <div>Loading...</div>
    );
  }
}